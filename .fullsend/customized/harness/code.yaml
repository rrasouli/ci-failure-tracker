---
# harness/code.yaml — Python/Flask code agent override.
#
# Inherits upstream code harness defaults. Overrides:
#   - plugins: pylsp instead of gopls-lsp
#   - env: python-agent.env instead of code-agent.env (Go-specific vars)
#   - skills: dashboard-specific skills added
#   - timeout: reduced (Python projects build faster than Go)
agent: agents/code.md
doc: docs/agents/code.md
model: opus
image: ghcr.io/fullsend-ai/fullsend-code:latest
policy: policies/code.yaml

role: coder
slug: fullsend-ai-coder

host_files:
  - src: env/gcp-vertex.env
    dest: /sandbox/workspace/.env.d/gcp-vertex.env
    expand: true
  - src: env/python-agent.env
    dest: /sandbox/workspace/.env.d/python-agent.env
    expand: true
  - src: ${GOOGLE_APPLICATION_CREDENTIALS}
    dest: /tmp/.gcp-credentials.json
  - src: ${GCP_OIDC_TOKEN_FILE}
    dest: /sandbox/workspace/.gcp-oidc-token
    optional: true

pre_script: scripts/pre-code.sh
post_script: scripts/post-code.sh

skills:
  - skills/code-implementation
  - skills/dashboard-context
  - skills/collector-patterns

plugins:
  - plugins/pylsp

env:
  runner:
    CODE_ALLOWED_TARGET_BRANCHES: "${CODE_ALLOWED_TARGET_BRANCHES}"
    FULLSEND_OUTPUT_SCHEMA: ${FULLSEND_DIR}/schemas/code-result.schema.json
    FULLSEND_OUTPUT_FILE: code-result.json
  sandbox:
    PATH: "/sandbox/workspace/venv/bin:$PATH"

timeout_minutes: 25

forge:
  github:
    pre_script: scripts/pre-code.sh
    post_script: scripts/post-code.sh
    env:
      runner:
        PUSH_TOKEN: "${PUSH_TOKEN}"
        PUSH_TOKEN_SOURCE: "${PUSH_TOKEN_SOURCE}"
        REPO_FULL_NAME: "${REPO_FULL_NAME}"
        ISSUE_NUMBER: "${ISSUE_NUMBER}"
        REPO_DIR: "${GITHUB_WORKSPACE}/target-repo"
