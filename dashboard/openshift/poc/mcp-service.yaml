apiVersion: v1
kind: Service
metadata:
  name: prow-mcp-server
  labels:
    app: prow-mcp-server
spec:
  selector:
    app: prow-mcp-server
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 8000
      name: http
  type: ClusterIP
