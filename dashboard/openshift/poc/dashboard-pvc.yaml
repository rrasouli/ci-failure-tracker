apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: winc-dashboard-poc-data
  labels:
    app: winc-dashboard-poc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
